# Troubleshooting

## Authentication Fails

Check that the key belongs to the provider selected by `ANTHROPIC_BASE_URL`.

## Model Not Found

Verify provider model IDs. Do not assume Anthropic model names work unchanged through every router.

## Tool Behavior Differs

Record the provider, model, request shape, and failing task in failure telemetry.

