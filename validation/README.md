# Validation

Check that examples use placeholders only, `.env` is ignored, and provider-specific docs identify limitations rather than promising universal compatibility.

## Repo-specific Demo Validation

Run:

```bash
python demo_env_profile_validator.py
```

This is the repo-specific fixture path. It is intentionally local-only.

